# Copy to .env (git-ignored) and fill in.
# Either use this, or store via `python scripts/setup_gmail_auth.py` (macOS Keychain).
# .env wins if both are set.

# 16-character Gmail app password (spaces OK, will be stripped).
GMAIL_APP_PASSWORD=
